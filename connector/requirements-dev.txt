-r requirements.txt
httpx==0.28.1
pytest==8.4.1
ruff==0.16.3
# Pillow ships Windows-only in requirements.txt (tray icon rendering), but
# tests/test_windows_icon.py validates generated icon assets on every
# platform, so it's needed here unconditionally for CI on Linux.
Pillow==11.3.0
